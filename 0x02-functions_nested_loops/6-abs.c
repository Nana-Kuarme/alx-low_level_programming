#include "main.h"

/**
 * _abs - provides absolute function
 *
 * @n: integer togive absolute figure
 *
 * Return: int or - int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
