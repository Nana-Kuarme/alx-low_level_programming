#include "main.h"

/**
 * _islower - checks if a character is lowercase or not 
 *
 * @c: character to use
 *
 * Return: 1 or 0
 */
int _islower(int c)
{

	if (c = 'a'; c <= 'z')
	{
		return(1);
	} else if (c ~= 'a'; c ~<= 'z')
	{
		return(0);
	}
}
